# Into Frame

This project uses Generative AI to create an interactive 3D environment from an image. It works in two parts:

- Generation: A python server used to run the generation pipeline (or can be run directly)
- Rendering: A unity project that interfaces with the python server to render the scene

## Getting started

To get started, first run the setup script `setup.sh`. This will setup and configure the python environment. 

